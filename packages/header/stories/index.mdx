import { Meta, Story, Preview, DocsStory } from '@storybook/addon-docs/blocks';

# Header

The header is similar to the intro but it's placed at the top and behaviors differently on mobile.

## Standard

<Preview withToolbar>
  <Story id="header--standard" />
</Preview>
